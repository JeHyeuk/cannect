from cannect.config import env
from pathlib import Path
import os


class Baseline:

    def __init__(self, baseline:str):


        # for root, dirs, files in os.walk(env.BASELINE_PATH):
        #     for dr in dirs:
        #         print(dr)
        return

if __name__ == "__main__":

    base = Baseline('TX4T9MTN901N')